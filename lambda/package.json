{
  "name": "lsatprep-lambda",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0"
  },
  "scripts": {
    "test": "node --test --test-reporter=spec src/lib/http.test.mjs src/integration.test.mjs"
  }
}
