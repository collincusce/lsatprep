{
  "name": "lsatprep-lambda",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0"
  },
  "scripts": {
    "test": "node --test src/**/*.test.mjs"
  }
}
